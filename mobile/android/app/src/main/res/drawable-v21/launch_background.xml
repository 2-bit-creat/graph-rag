<?xml version="1.0" encoding="utf-8"?>
<!-- The launch window is shown before Flutter draws its first frame.

     It used to inherit the platform theme (?android:colorBackground, i.e.
     Theme.Light/Theme.Black), but the app hard-defaults to ThemeMode.dark
     regardless of the system setting — so on a phone in light mode the launch
     window was white and then flipped to the dark app. Pin it to the app's own
     background instead.

     The hub mark is drawn on top so a cold start reads as Daynode starting
     rather than as an empty black window. (Android 12+ ignores this and builds
     its splash from the adaptive icon, which carries the same mark.) -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher_foreground" />
    </item>
</layer-list>
